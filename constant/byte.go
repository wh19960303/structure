package constant

const (
	Byte = iota + 1
	KB   = Byte * 1024
	MB   = KB * 1024
	GB   = MB * 1024
	TB   = GB * 1024
	PB   = TB * 1024
	EB   = PB * 1024
	ZB   = EB * 1024
	YB   = ZB * 1024
	BB   = YB * 1024
)
